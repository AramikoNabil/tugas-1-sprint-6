import React, {Component} from 'react';
import {Text, View} from 'react-native';

export default class screen3 extends Component {
  render() {
    // const {Send} = this.props.route.params;
    return (
      <View>
        {/* <Text> kiriman : {this.props.route.params.Send}</Text> */}
      </View>
    );
  }
}
